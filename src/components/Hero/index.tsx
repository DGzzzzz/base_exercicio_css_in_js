import { FormDiv, HeroContainer, HeroTitle } from './styles'

const Hero = () => (
  <HeroContainer>
    <FormDiv>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </FormDiv>
  </HeroContainer>
)

export default Hero
